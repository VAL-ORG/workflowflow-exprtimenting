name: seting-variable

on:
  workflow_dispatch:

env:
  MY_VAR: "Hello from workflow-jelena"

jobs:
  show-variable:
    runs-on: ubuntu-latest

    steps:
      - name: Show variable value -step 1
        run: echo something
      - name: Show variable value -step2
        run: echo "Value of MY_VAR is: $MY_VAR"
