name: Test Variable

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Print variable in bash
        env:
          MY_VAR: "Hello from workflow"
        run: echo "Bash: $MY_VAR"


