name: seting-variable

on:
  workflow_dispatch:

env:
  MY_VAR: "Hello from workflow-jelena"

jobs:
  show-variable:
    runs-on: ubuntu-latest

    steps:
      - name: Show variable value
        run: echo something
